#include "holberton.h"
/**
 * add - adds two integers and returns the result
 * @n: get a number
 * @m: get a number
 * Return: return 0
 */
int add(int n, int m)
{
	int o;

	o = n + m;
	return (o);
}
